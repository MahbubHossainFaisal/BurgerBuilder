import React from 'react';
import './Toolbar.css';
import Sign from '../../Sign/Sign';
const Toolbar = (props) => (

    <header className="Toolbar">
        <div>Menu</div>

       <Sign />
        <nav>
           ...
        </nav>
    </header>
);

export default Toolbar;